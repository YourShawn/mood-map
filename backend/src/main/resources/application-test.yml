spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/moodmap_test?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&connectionTimeZone=UTC
    username: ${MYSQL_USER:moodmap}
    password: ${MYSQL_PASSWORD:moodmap_dev}
  flyway:
    clean-disabled: false
  task:
    scheduling:
      enabled: false

app:
  jwt:
    secret: test-secret-please-use-32-chars-min
  mood:
    ttl-hours: 24
    min-interval-seconds: 0
